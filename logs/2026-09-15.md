# 夜班日志 2026-09-15（周二）

## 班次：出版署（08:50 同步窗口）

- **08:50** 开工扫描：工作区干净（`nothing to commit`），本地领先 `origin/main` 7 个提交——全部是 09-12 夜班 A/B/C 的成果，自 `6c89be2` 起一直没有推送。SSH 远端可达，可快进推送。
- **08:51** 发布前检查：7 个提交涉及 17 个文件（README/日志/项目状态/六页 site/五个工具/测试），diff 规模 +3476/−567。密钥初筛（对完整补丁扫描 API Key/Token/密码/私钥/SSH/Cookie/Session 等模式）0 命中；无临时文件、缓存、构建垃圾（`.render-tmp` 为本班自建并已删除，`__pycache__` 由 .gitignore 覆盖）。
- **08:52** 测试：初始 78/78 通过；`memory_check` 正常（0 问题、0 待人工确认）。
- **08:53** 视觉检查：用真实 Chrome（无头 Playwright）逐页渲染 site/ 六页并捕获 console。**发现 P1 真缺陷**：`sky.html` 抛 `esc is not defined`，整页只剩标题与导航、画布全黑（body 文本仅 40 字符）——`night_sky.py` 生成器里 `esc()` 被调用却从未定义，悬停提示与筛选按钮两条路径都会撞上；且脚本在顶层 `filters.innerHTML` 处即抛错，星图（canvas）从未绘制。
- **08:54** 修复 P1：`tools/night_sky.py` 模板补上 `esc` 定义（与其余页面同款转义表）。重出页面后 body 文本 40→86，console 干净，星图正常绘制。
- **08:54** 视觉复检又发现两处：(1) `sky.html` 筛选项「未标记 未标记」标签重复——`laneNames["未标记"] = "未标记"` 自我映射所致；(2) `docs.html` 在**约定的 GitHub Pages 模式**（Pages 选 `site/` 目录为根）下点任何文档均 404——`fetch("../" + path)` 跳出了发布根之外（`file://` 同样被 CORS 拦），主内容区永远空白。
- **08:55** 修复 docs：正文随页面内嵌（`night_docs.py` 采集时读取文本注入 `const CONTENT`），`openDoc` 只用内嵌正文，彻底不依赖相对路径。`file://` 双击、`--serve`、Pages 选 `site/` 三种方式都能读；README 与脚本注释同步更正。实测两模式均渲染 4169 字符正文。
- **08:56** 回归测试：新增 2 项（`esc` 必须定义 + 筛选标签不重复；正文内嵌且占位符已替换）。
- **08:58** 视觉终检（子代理独立判定）：六页复检 5 页 pass，`index.html` 判 fail——**是本班自己引入的回归**：我按默认参数重出驾驶舱，其默认窗口是「最近一次 23:05 → 现在」，昨夜无班，时间轴与班次贡献表被重出成空。C 原版页面记录的是有班次的窗口。改以覆盖真实工作窗口重出（页面自报窗口，仍如实）。
- **08:59** 同轮还查出导航缺口：`index.html` 与 `history.html` 缺星图链接，星图页面无法从首页到达。三处生成器（night_web/night_docs/night_history）补齐互链，并加跨页导航回归测试（六页必须两两互链）。
- **09:00** 顺着同一镜头审计全站相对路径，发现**同类根逃逸**：`index.html`「最新交接文档/最新夜班日志」入口用 `href="../handoff/..."`——`--serve`（仓库根）下可用，但 Pages 选 `site/` 为根时 404。改为深链 `docs.html#<路径>`（图书馆正文已内嵌，最稳），`openDoc` 支持 hash 直达。
- **09:01** 深链联调又暴露跨模块不一致：驾驶舱列出的「晨报」是生成物，图书馆按既定口径不收，深链落空。`latest_files` 补上同一排除规则（与 memory_check/night_docs 一致），并加「驾驶舱入口必须存在于图书馆清单」的一致性测试。
- **09:02** 回归测试净增 6 项，84/84 通过；memory_check 0 问题。视觉终检六页全 pass。
- **09:03** 提交并推送 `origin/main`。

### 发布结果

- 推送内容：夜班 C 的 site/ 六页体系（驾驶舱/图书馆/大事记/出勤表/星图/模拟器）+ 五个生成工具 + 出版署的缺陷修复。
- 本班新增提交：`night_sky.py` 补 `esc`/去重筛选标签；`night_docs.py` 正文内嵌 + hash 深链；`night_web.py` 补星图导航 + 文档入口改深链 + 排除生成物；`night_history.py` 补星图导航；README 更正；6 项回归测试；本日志与项目状态更新。
- 说明：修复均属「让已发布内容真正可用」，未扩大功能范围。星图 x 轴呈现的「左右两簇星、中间留白」经核实是真实时间间隙（夜间睡眠），非缺陷，故不改。
